<Application x:Class="P2PFT_Cs.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:P2PFT_Cs"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- ══════════════════════════════════════════════════════
             Global colour palette (mirrors style.css :root vars)
             ══════════════════════════════════════════════════════ -->
        <Color x:Key="BgBodyColor">#F5F6F8</Color>
        <Color x:Key="BgSidebarColor">#1E2330</Color>
        <Color x:Key="BgCardColor">#FFFFFF</Color>
        <Color x:Key="BgHoverColor">#F0F2F5</Color>
        <Color x:Key="BgInputColor">#F5F6F8</Color>
        <Color x:Key="BorderColor">#E2E5EA</Color>
        <Color x:Key="BorderFocusColor">#4F7DF7</Color>
        <Color x:Key="TextPrimaryColor">#1A1D26</Color>
        <Color x:Key="TextSecondaryColor">#636878</Color>
        <Color x:Key="TextMutedColor">#9CA0AD</Color>
        <Color x:Key="TextSidebarColor">#B8BCC8</Color>
        <Color x:Key="AccentColor">#4F7DF7</Color>
        <Color x:Key="AccentHoverColor">#3D6BE5</Color>
        <Color x:Key="SuccessColor">#22C55E</Color>
        <Color x:Key="SuccessBgColor">#ECFDF5</Color>
        <Color x:Key="WarningColor">#EAB308</Color>
        <Color x:Key="WarningBgColor">#FEFCE8</Color>
        <Color x:Key="ErrorColor">#EF4444</Color>
        <Color x:Key="ErrorBgColor">#FEF2F2</Color>
        <Color x:Key="InfoColor">#3B82F6</Color>
        <Color x:Key="InfoBgColor">#EFF6FF</Color>
        <Color x:Key="DangerHoverColor">#DC2626</Color>

        <SolidColorBrush x:Key="BgBodyBrush"        Color="{StaticResource BgBodyColor}"/>
        <SolidColorBrush x:Key="BgSidebarBrush"     Color="{StaticResource BgSidebarColor}"/>
        <SolidColorBrush x:Key="BgCardBrush"        Color="{StaticResource BgCardColor}"/>
        <SolidColorBrush x:Key="BgHoverBrush"       Color="{StaticResource BgHoverColor}"/>
        <SolidColorBrush x:Key="BgInputBrush"       Color="{StaticResource BgInputColor}"/>
        <SolidColorBrush x:Key="AppBorderBrush"     Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="BorderFocusBrush"   Color="{StaticResource BorderFocusColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush"   Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="TextMutedBrush"     Color="{StaticResource TextMutedColor}"/>
        <SolidColorBrush x:Key="TextSidebarBrush"   Color="{StaticResource TextSidebarColor}"/>
        <SolidColorBrush x:Key="AccentBrush"        Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentHoverBrush"   Color="{StaticResource AccentHoverColor}"/>
        <SolidColorBrush x:Key="SuccessBrush"       Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="SuccessBgBrush"     Color="{StaticResource SuccessBgColor}"/>
        <SolidColorBrush x:Key="WarningBrush"       Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="WarningBgBrush"     Color="{StaticResource WarningBgColor}"/>
        <SolidColorBrush x:Key="ErrorBrush"         Color="{StaticResource ErrorColor}"/>
        <SolidColorBrush x:Key="ErrorBgBrush"       Color="{StaticResource ErrorBgColor}"/>
        <SolidColorBrush x:Key="InfoBrush"          Color="{StaticResource InfoColor}"/>
        <SolidColorBrush x:Key="InfoBgBrush"        Color="{StaticResource InfoBgColor}"/>
        <SolidColorBrush x:Key="DangerHoverBrush"   Color="{StaticResource DangerHoverColor}"/>

        <!-- ── Value converters ───────────────────────────────── -->
        <local:BoolToVisibilityConverter        x:Key="BoolToVis"/>
        <local:InverseBoolToVisibilityConverter  x:Key="InverseBoolToVis"/>
        <local:StatusLevelToBrushConverter       x:Key="StatusBgConv"/>
        <local:StatusLevelToForegroundConverter   x:Key="StatusFgConv"/>
        <local:PeerStatusToBrushConverter        x:Key="PeerDotConv"/>
        <local:UnixTimestampToStringConverter    x:Key="TimestampConv"/>
        <local:FileSizeConverter                 x:Key="FileSizeConv"/>

        <!-- ── Rounded button base style ──────────────────────── -->
        <Style x:Key="BtnBase" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding"    Value="14,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnPrimary" TargetType="Button" BasedOn="{StaticResource BtnBase}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="BtnSecondary" TargetType="Button" BasedOn="{StaticResource BtnBase}">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"       Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="BorderBrush"      Value="{StaticResource AppBorderBrush}"/>
        </Style>
        <Style x:Key="BtnSuccess" TargetType="Button" BasedOn="{StaticResource BtnBase}">
            <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="BtnDanger" TargetType="Button" BasedOn="{StaticResource BtnBase}">
            <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="BtnSmall" TargetType="Button" BasedOn="{StaticResource BtnBase}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding"  Value="10,4"/>
        </Style>
        <Style x:Key="BtnIcon" TargetType="Button" BasedOn="{StaticResource BtnBase}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSidebarBrush}"/>
            <Setter Property="FontSize"   Value="16"/>
            <Setter Property="Padding"    Value="6,4"/>
        </Style>

        <!-- ── Card container ─────────────────────────────────── -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background"      Value="{StaticResource BgCardBrush}"/>
            <Setter Property="BorderBrush"      Value="{StaticResource AppBorderBrush}"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="CornerRadius"     Value="10"/>
            <Setter Property="Margin"           Value="0,0,0,12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="6" ShadowDepth="1"
                                      Opacity="0.06" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── PasswordBox style ──────────────────────────────── -->
        <Style x:Key="InputPassword" TargetType="PasswordBox">
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Padding"         Value="10,8"/>
            <Setter Property="Background"      Value="{StaticResource BgInputBrush}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource AppBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground"      Value="{StaticResource TextPrimaryBrush}"/>
        </Style>
    </Application.Resources>
</Application>
